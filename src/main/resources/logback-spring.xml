<configuration>

    <!-- Define a timestamp format -->
    <timestamp key="byDay" datePattern="yyyy-MM-dd"/>

    <!-- Rolling File Appender -->
    <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Log file location -->
        <file>logs/ezcleannj.log</file>
        
        <append>false</append> <!-- Ensures file is wiped on every run -->

        <!-- Rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Rotate logs daily -->
            <fileNamePattern>logs/ezcleannj.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep logs for 30 days -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        
        <!-- Log message format -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger Configuration -->
    <root level="INFO">
        <appender-ref ref="ROLLING" />
    </root>

    <!-- Package-Specific Logger Configuration -->
    <logger name="org.springframework.web" level="DEBUG" />

</configuration>
